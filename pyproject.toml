[project]
name = "quick-agentic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RdlphESTP", email = "pro.rdlph@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "chainlit>=2.11.1",
    "langgraph>=1.2.11",
]

[project.scripts]
quick-agentic = "quick_agentic:main"

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
